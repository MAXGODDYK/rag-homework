Repair the model output below.

Return exactly one valid JSON object with this schema:
{"answer":"string","citations":["chunk_id"],"insufficient_context":false}

Rules:
- citations may contain only IDs from ALLOWED_CHUNK_IDS;
- a supported answer must contain at least one citation;
- if the context is insufficient, use FALLBACK_SENTENCE exactly,
  set insufficient_context to true, and return an empty citations list;
- return no Markdown and no text outside JSON.

ALLOWED_CHUNK_IDS:
{{ALLOWED_CHUNK_IDS}}

FALLBACK_SENTENCE:
{{FALLBACK}}

INVALID_OUTPUT:
{{INVALID_OUTPUT}}
